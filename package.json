{
  "name": "obsidian-latex-color-sync",
  "version": "0.3.1",
  "private": true,
  "description": "Build tooling for Obsidian LaTeX Color Sync plugin.",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "build:dev": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
